  var main = "sdfstosdfoodfpsssdfrrr"

  var pattern = "topper"  

  var status ="pattern did not match";

  for (var i = 0; j = 0; i < main.length; i++) {
      if (main.charAt(i) == pattern.charAt(j)) {
          j++;
      
  }

   if (j == pattern.length)
    status = "pattern macthed";
  
} 
 console.log(status);